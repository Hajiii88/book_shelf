<% if obj.errors.any? %>
  <div class="alert alert-danger">
    <p><b><%= obj.errors.count  %>件のエラーが発生しました。</b></p>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
